import type { Actions } from "./$types";

export const actions: Actions = {
  login: (e) => {
    console.log(e);
  },
  logout: async (e) => {
    console.log(e);
  },
  signup: async (e) => {
    console.log(e);
  }
};
